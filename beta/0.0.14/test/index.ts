import repl from "../repl"
repl()

//import { HydroScriptSyntaxError } from "../error"
//import { TokenType } from "../lexer/token_type"
//throw new HydroScriptSyntaxError(TokenType.Await, TokenType.Return, "<unknown>", 4, 4)

//throw new Error("test")

//const HydroScript = require("../.compiled")
//HydroScript.compile(";")