declare module "hydroscript/run" {

    /**
     * Compiles a HydroScript file to JavaScript and stores it in current directory, then runs it and deletes it
     * Parameters should be specified when running console command
     */
    export default function run(): void

}